<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/drawer_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fitsSystemWindows="true"
        tools:openDrawer="start">

    <!-- Menú lateral -->
    <com.google.android.material.navigation.NavigationView
            android:id="@+id/nav_view"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="start"
            android:background="@color/white"
            android:fitsSystemWindows="true"
            app:headerLayout="@layout/nav_header"
            app:menu="@menu/nav_menu"/>

    <!-- Contenido principal de la actividad -->
    <FrameLayout
            android:id="@+id/container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/white"
            android:padding="16dp">

        <!-- ImageView para mostrar la imagen explicativa -->
        <ImageView
                android:id="@+id/image_view_explicativo_suma"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:scaleType="fitCenter"
                android:src="@drawable/imagen_suma"
                android:contentDescription="@string/imagen_explicativa_suma"/>
        <ImageView
                android:id="@+id/image_view_explicativo_resta"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:scaleType="fitCenter"
                android:src="@drawable/imagen_resta"
                android:contentDescription="@string/imagen_explicativa_resta"/>
        <ImageView
                android:id="@+id/image_view_explicativo_multiplicacion"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="fitCenter"
                android:src="@drawable/imagen_multiplicacion"
                android:contentDescription="@string/imagen_explicativa_multiplicacion"/>
    </FrameLayout>

</androidx.drawerlayout.widget.DrawerLayout>
